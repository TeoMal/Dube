import App from './App';
import { AppRegistry } from 'react-native';

AppRegistry.registerComponent('Dube', () => App);
AppRegistry.runApplication('Dube', {
  rootTag: document.getElementById('root')
});
